import {Text} from 'react-native';
import {SignedInWrapper} from '../SignedInWrapper/SignedInWrapper';

export const Details = () => {
  return (
    <SignedInWrapper>
      <Text>Details</Text>
    </SignedInWrapper>
  );
};
